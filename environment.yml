name: int_grad_corr
channels:
    - pytorch
    - defaults
dependencies:
    - python=3.10
    - jupyter
    # Sciences
    - numpy
    - matplotlib
    - scikit-learn
    # Neural Networks
    - pytorch::pytorch=2.4  # Mac Intel limitated to 2.2
    - pytorch::torchvision
    - tensorboard
    # Image
    - pillow
    # Others
    - h5py
    - tqdm
